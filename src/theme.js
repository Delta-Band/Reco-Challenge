import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1F5CED'
    },
    secondary: {
      main: '#FFFFFF'
    }
  },
  space: {
    lg: 24
  }
});

export default theme;
